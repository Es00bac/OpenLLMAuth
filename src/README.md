# Source Tree

The active package in this directory is [`open_llm_auth`](open_llm_auth).

Public setup, configuration, API, provider, and security documentation lives in
the repository-level [README](../README.md) and the [`docs`](../docs) directory.
This file exists only to keep the source tree self-describing when browsed
directly.
